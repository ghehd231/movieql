type Movie {
  id: Int!
  name: String!
  score: Int!
}

type Query {
  #   people: [Person]!
  #   person(id: Int!): Person
  movies: [Movie]!
  movie(id: Int!): Movie
}

#mutation은 database 상태가 변할 때 사용
type Mutation {
  addMovie(name: String!, score: int!): Movie!
}
